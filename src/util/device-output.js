
// @param {string|array} ctrl
export function press(ctrl) {
  console.log(`output.PRESS: ${ctrl}`);
}

// @param {string|array} ctrl
export function release(ctrl) {
  console.log(`output.RELEASE: ${ctrl}`);
}